<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".authentication.login.LoginActivity">

    <ImageView
        android:id="@+id/iv_LoadingPage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="100dp"
        android:layout_marginTop="250dp"
        android:layout_marginEnd="100dp"
        android:src="@drawable/zyro_logo" />

    <TextView
        android:id="@+id/tv_LoadingPage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/pb_LoadingPage"
        android:layout_centerHorizontal="true"
        android:text="0 %"
        android:textColor="@color/black"
        android:textSize="16dp" />

    <ProgressBar
        android:id="@+id/pb_LoadingPage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_LoadingPage"
        android:indeterminate="true"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_marginEnd="70dp"
        android:layout_marginStart="70dp"
        android:indeterminateTint="@color/neonblue" />

</RelativeLayout>